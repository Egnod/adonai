version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: adonai_password
      POSTGRES_USER: adonai_user
      POSTGRES_DB: adonai